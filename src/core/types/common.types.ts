export type UUID = string;
export type Timestamp = number;

export type ScreenType = 'dashboard' | 'editor' | 'login' | 'register';
export type PreviewMode = 'side-by-side' | 'preview-only' | 'edit-only';
